import React from "react";
import NavBar from "./NavBar";

const HeroSection = () => {
  return (
    <section className="bg-hero-pattern bg-cover bg-center h-screen">
        <NavBar/>
    </section>
  );
};

export default HeroSection;
